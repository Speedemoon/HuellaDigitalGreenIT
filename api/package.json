{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build": "node -e \"require('fs').mkdirSync('public',{recursive:true})\" && npm --prefix ../web/web install && npm --prefix ../web/web run build && rimraf public/* && cpx \"../web/web/dist/web/browser/**/*\" public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mysql2": "^3.16.1"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "nodemon": "^3.1.11",
    "rimraf": "^6.0.1"
  }
}
